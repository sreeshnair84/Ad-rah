# MongoDB and async dependencies for OpenKiosk
motor>=3.3.0              # Async MongoDB driver
pymongo>=4.5.0            # MongoDB synchronous driver (used by motor)
dnspython>=2.4.0          # For MongoDB SRV connection strings

# Existing dependencies (ensure these are installed)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
aiofiles>=23.2.1